import {provide, Provider} from 'angular2/core';
import ManagedStore from './managedStore';
import Connector from './connector';

let connector = null;

/**
 * Provides all related redux providers to the DI system.
 * @returns {Provider}
 */
export const REDUX_PROVIDERS = [
  provide(Connector, {useFactory: () => new Connector()})
];