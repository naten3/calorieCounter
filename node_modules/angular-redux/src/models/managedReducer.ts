import {ActionCreator} from './actions';
import {ManagedReducer} from '../core/managedReducer';
import {Reducer} from 'redux';

export interface IManagedReducerActionArgs {
  creator: ActionCreator;
  handler: Function;
}

export class ManagedReducerActionArgs implements IManagedReducerActionArgs {
  constructor(public creator:ActionCreator, public handler: Function) {};
}